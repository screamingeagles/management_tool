@model IEnumerable<ManagementTool.Models.C010_STATUS>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TaskStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TaskActive)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TaskCreatedBy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TaskCreatedDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TaskStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TaskActive)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TaskCreatedBy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TaskCreatedDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StatusId })
        </td>
    </tr>
}

</table>
